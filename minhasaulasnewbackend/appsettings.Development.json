{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "ConnectionPostgres": "server=${DB_HOST}; port=${DB_PORT}; user id=${DB_USER}; password=${DB_PASS}; database=${DB_NAME};"
  }
}
